server {
  listen 80;
  
  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    # This is the key for SPAs: if a file is not found, fall back to index.html
    try_files $uri $uri/ /index.html;
  }
}
